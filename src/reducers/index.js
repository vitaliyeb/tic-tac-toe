const initialState = {
    firstLoading: true,
    authentication: false,
    roomList: []
}

const reducer= (state = initialState, action) => {
    switch(action.type) {

        default:
            return state;
    }
}
 


export default reducer